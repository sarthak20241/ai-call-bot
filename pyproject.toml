[project]
name = "ai-call"
version = "0.1.0"
description = "AI-powered outbound calling system with real-time speech-to-speech conversation"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.121.0",
    "livekit-agents[google]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.38.0",
]
